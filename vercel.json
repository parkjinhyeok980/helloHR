{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "cd FE && npm ci",
  "buildCommand": "cd FE && npm run build",
  "outputDirectory": "FE/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" }
  ]
}
